# Standalone trainers app
#
# Copy this file to `.env` (do not commit `.env`).

# DB (local docker Postgres)
DATABASE_URL="postgresql://trainer:trainer@localhost:5435/symmetry_trainer?schema=public"

# Auth
AUTH_COOKIE_NAME="smmtry_trainer_sess"
AUTH_SESSION_TTL_DAYS="30"

