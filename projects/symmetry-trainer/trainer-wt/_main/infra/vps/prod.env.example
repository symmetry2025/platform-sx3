# math-trainer.ru — PROD (VPS)
#
# Copy to /opt/math_trainer/prod/.env (do not commit).

# Image to pull from GHCR
IMAGE="ghcr.io/symmetry2025/math_trainer/trainer-standalone"
# Prod deploy uses git tag (workflow passes APP_TAG=<tag>), but keep a default.
APP_TAG="latest"
# Local bind port for nginx upstream
APP_PORT="24015"

# Postgres
POSTGRES_DB="math_trainer_prod"
POSTGRES_USER="math_trainer"
POSTGRES_PASSWORD="CHANGE_ME_STRONG_PASSWORD"

# Prisma/Next.js DB connection (must point to docker service name `db`)
DATABASE_URL="postgresql://math_trainer:CHANGE_ME_STRONG_PASSWORD@db:5432/math_trainer_prod?schema=public"

# Auth cookies (prod)
AUTH_COOKIE_NAME="math_trainer_prod_sess"
AUTH_SESSION_TTL_DAYS="30"

