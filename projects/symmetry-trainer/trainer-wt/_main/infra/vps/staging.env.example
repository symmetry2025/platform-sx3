# math-trainer.ru — STAGING (VPS)
#
# Copy to /opt/math_trainer/staging/.env (do not commit).

# Image to pull from GHCR
IMAGE="ghcr.io/symmetry2025/math_trainer/trainer-standalone"
# Staging deploy always tracks tag `staging`
APP_TAG="staging"
# Local bind port for nginx upstream
APP_PORT="14015"

# Postgres
POSTGRES_DB="math_trainer_staging"
POSTGRES_USER="math_trainer"
POSTGRES_PASSWORD="CHANGE_ME_STRONG_PASSWORD"

# Prisma/Next.js DB connection (must point to docker service name `db`)
DATABASE_URL="postgresql://math_trainer:CHANGE_ME_STRONG_PASSWORD@db:5432/math_trainer_staging?schema=public"

# Auth cookies (keep different from prod)
AUTH_COOKIE_NAME="math_trainer_staging_sess"
AUTH_SESSION_TTL_DAYS="30"

